import { FC } from 'react';

interface LoaderProps {
    className?: string;
}

export const L123: FC<LoaderProps> = (props) => {
    const { className } = props;

    return (
        <div />
    );
};
export default L123;
